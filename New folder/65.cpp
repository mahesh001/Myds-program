#include<iostream>
using namespace std;
struct htnode
{
    int size;
    int* H;
};
void swap(int* a, int* b)
 { int t = *a;
  *a = *b; 
   *b = t; 
   }
   void heafify(int H[],int i)
   {
   	min(H[2i],H[2i+1],j)
   	if(H[i]<H[j/2])
   return	swap(H[i],H[j/2]);
   }
   void heapsort(htnode*T)
   {
   	int i;
   	char a[i];
   	if(a[i])
   }
