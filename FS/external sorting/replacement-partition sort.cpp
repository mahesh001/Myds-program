#include<iostream>
#include<fstream>
using namespace std;
int min(int a[],int n)
{    int min=999;
	for(int i=0;i<n;i++)
	if(a[i]<min);
	return a[i];
	else
	 return 
	
}
int main()
{
	ifstream fin;
	fin.open("replace.txt");
	ofstream fout;
	fout.open("output.txt");
	int a[100],n;
	cout<<"enter the partion array size \n";
	cin>>n;
	for(int i=0;i<n;i++ )
	cin>>a[i];
	int l;
	
}
